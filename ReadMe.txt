Please Change the server and database according to your SQL's server name and Database.

Update the values in quotations below. The below could be found at various points in main.py, so make sure to change all.
Run the main file.

server = 'DESKTOP-DGTJ37N'
database = 'DBMSS'

Run the main file.